{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"Customer Feedback ML prototype. Currently Phase 1 (sentiment analysis: positive/negative/neutral). Working agreement: keep code simple and readable, notebook-first, and do NOT start coding a step until the user says go. Authoritative plan: PROJECT_SETUP.md.\"}}'",
            "statusMessage": "Loading project context"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.py) python3 -m py_compile \"$f\" 2>/dev/null && echo \"{\\\"systemMessage\\\":\\\"py_compile OK: $f\\\"}\" || echo \"{\\\"systemMessage\\\":\\\"Python syntax error in $f - fix before continuing\\\"}\";; esac; } 2>/dev/null || true",
            "statusMessage": "Syntax-checking Python"
          }
        ]
      }
    ]
  }
}
